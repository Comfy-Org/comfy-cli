{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://comfy.org/schemas/build_delete.json",
  "title": "comfy build delete --json data payload",
  "type": "object",
  "required": ["buildId", "distributionId", "deleted"],
  "additionalProperties": true,
  "properties": {
    "buildId": {"type": "string", "description": "The deleted build's id."},
    "distributionId": {"type": "string", "description": "Deprecated alias of `buildId`, always identical to it. Emitted for one release so pinned scripts keep parsing; prefer `buildId`."},
    "deleted": {"type": "boolean"}
  }
}
