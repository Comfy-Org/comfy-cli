{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://comfy.org/schemas/build_blob_ls.json",
  "title": "comfy build blob ls --json data payload",
  "type": "object",
  "required": ["blobs", "truncated"],
  "additionalProperties": true,
  "properties": {
    "blobs": {"type": "array", "items": {"type": "object", "additionalProperties": true}},
    "truncated": {"type": "boolean", "description": "The builder serves this listing as a single page with no cursor, so older blobs past its page limit are unreachable from this command. True when the returned page is full and the workspace may therefore hold blobs this payload does not list."}
  }
}
