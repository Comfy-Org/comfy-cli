{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://comfy.org/schemas/distribution_version_logs.json",
  "title": "comfy distribution version logs --json data payload",
  "description": "One build target's captured log, as returned by GET /v1/distribution-versions/{id}/logs.",
  "type": "object",
  "required": ["versionId", "log", "truncated"],
  "additionalProperties": true,
  "properties": {
    "versionId": {"type": "string"},
    "os": {"type": "string", "enum": ["linux", "windows", "mac"]},
    "gpu": {"type": "string", "enum": ["nvidia", "amd", "cpu", "mps"]},
    "log": {"type": "string"},
    "truncated": {"type": "boolean"}
  }
}
